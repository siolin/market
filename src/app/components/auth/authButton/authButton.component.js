import {AuthButtonController} from './authButton.controller';
import authButtonTemplate from './authButton.html';

export const authButton = {
  template: authButtonTemplate,
  controller: AuthButtonController
};
